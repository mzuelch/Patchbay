# GUI-only dependencies (in addition to the backend ML stack)
#
# Install as extras if you installed the package:
#   pip install -e ".[gui]"
#
# Or install manually:
#   pip install -r requirements-gui.txt

dearpygui>=1.10
numpy
sounddevice>=0.4.6
soundfile>=0.12.1
